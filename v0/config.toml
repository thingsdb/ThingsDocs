baseURL = "https://docs.thingsdb.net/v0/"
languageCode = "en-us"
title = "ThingsDB documentation"
publishDir = "../docs/v0"

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-learn"

# For search functionality
[outputs]
  home = [ "HTML", "RSS", "JSON"]

[params]
  # Change default color scheme with a variant one. Can be "red", "blue", "green".
  themeVariant = "mine"
  # Description of the site, will be used in meta information
  description = "ThingsDB documentation"
  disableInlineCopyToClipBoard = true
  obsolete = true

[[menu.shortcuts]]
name = "<i class='far fa-window-maximize'></i> Website"
url = "https://thingsdb.net/"
weight = 10

[[menu.shortcuts]]
name = "<i class='fab fa-github'></i> Github repo"
identifier = "ds"
url = "https://github.com/thingsdb/ThingsDB"
weight = 20